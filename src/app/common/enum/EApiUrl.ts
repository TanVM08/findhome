export enum DISTRICT {
    GET_ALL_DATA = "district/get-all-data",
}

export enum WARD {
    GET_DATA_BY_DISTRICT = "ward/",
}

export enum CATEGORY {
    GET_TYPE_HOUSE = "category/channel-group",
}

export enum ROOMS {
    CREATE_OR_UPDATE = "rooms/create-or-update",
}